# Copy this file to .env and fill in your values.
# Do not commit .env (it is in .gitignore).

# Required for LLM-based labeling and extraction (brands, purpose, themes).
OPENAI_API_KEY=

# Optional: for higher rate limits when loading WildChat from Hugging Face Hub.
# HF_TOKEN=
